import { HybridHero } from "../components/ui/hybrid-hero";

const Hero = () => {
  return (
    <section id="hero" className="w-full relative">
      <HybridHero />
    </section>
  );
};

export default Hero;
